# Configuración de BLACKBOX AI en VSCode

## 1. Establecer BLACKBOX AI como chat de IA predeterminado

1. Abre VSCode
2. Presiona `Ctrl+L` para abrir el panel de BLACKBOX AI
3. En la configuración de VSCode, ve a `File > Preferences > Settings`
4. Busca "AI Chat" o "Default Chat Provider"
5. Selecciona "BLACKBOX AI" como proveedor predeterminado

## 2. Configurar atajos de teclado

1. Ve a `File > Preferences > Keyboard Shortcuts` (o presiona `Ctrl+K Ctrl+S`)
2. Busca los siguientes atajos de BLACKBOX AI:
   - `blackbox.chat.focus` - Abrir el chat
   - `blackbox.inline.start` - Comandos inline
3. Haz clic en el icono de edición junto a cada comando para asignar los atajos deseados

## 3. Habilitar completado automático de código

1. Ve a `File > Preferences > Settings`
2. Busca "BLACKBOX AI" en la barra de búsqueda
3. Habilita las siguientes opciones:
   - `Enable Autocomplete` - Completado automático de código
   - `Enable Inline Completion` - Completado en línea mientras escribes
   - `AI Code Completion` - Selecciona BLACKBOX como proveedor

## Atajos recomendados

| Acción | Atajo |
|--------|-------|
| Abrir chat | `Ctrl+L` |
| Completar código | `Tab` o `Ctrl+Space` |
| Comandos inline | `Ctrl+I` |
